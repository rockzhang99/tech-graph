# fireworks-tech-graph 技术规范

> 分析日期：2026-08-30
> 仓库：https://github.com/yizhiyanhua-ai/fireworks-tech-graph
> 版本：v1.2.0（2026-07-17）
> License：MIT

---

## 1. 项目本质

`fireworks-tech-graph` 是一个 **Agent Skill + CLI 工具包**，供 **Codex 与 Claude Code 双端共用**。用途：把用户的自然语言系统/工作流描述，转化为**经过几何校验的 SVG 技术图**，并可导出：

- 高分辨率 **PNG**（默认 1920px，CairoSVG 渲染）
- **SVG → GIF 语义动效**（聚焦链路：仅接收生成器产出的语义 SVG，仅输出 960px / 20fps / 115 帧（5.75s）GIF + JSON motion report）
- **离线交互 HTML**（支持平移/缩放/主题切换/复制/SVG·PNG·JPEG·WebP 最高 4× 导出）

**定位边界**：仅用于技术图（架构、数据流、流程图、时序图、C4、云部署、事件流、可观测性排查、UML、ER、网络拓扑、时间线、概念图等）；不适用于照片、位图插画、量化数据图表。

## 2. 技术栈

| 类别 | 技术 | 版本/说明 |
|---|---|---|
| 核心 CLI | Python 3 | 统一入口 `scripts/fireworks.py`；要求 Python 3.9+（CI 覆盖 3.9 / 3.12） |
| 运行时 | Node.js | `svg2png.js` / `svg2gif.js`；要求 Node 18+（package.json engines） |
| GIF 渲染 | Puppeteer-core + Chromium | CI 中固定 `puppeteer-core@25.3.0`；真实 Chromium 跑 12 风格渲染 |
| PNG 渲染 | CairoSVG | 固定 `CairoSVG==2.7.1`；兜底 `rsvg-convert` |
| GIF 编码/校验 | ffmpeg、ImageMagick | CI 中 `apt-get install ffmpeg imagemagick`；FFprobe 校验产物 |
| 测试 | Python 内置 unittest | 9 个测试文件，无第三方测试框架 |
| 包管理/发布 | npm | 包名 `@yizhiyanhua-ai/fireworks-tech-graph`，`bin: fireworks-tech-graph` |
| CI/CD | GitHub Actions | CI / Release / 分发校验 / Pages |
| 文档站点 | GitHub Pages | 纯静态（仓库根 `index.html`），无构建步骤 |
| 安装分发 | Agent Skills 布局 | `npx skills add yizhiyanhua-ai/fireworks-tech-graph`，`SKILL.md` 为共用入口 |

**无后端、无数据库、无长连接、无 GUI、无 secrets** —— 纯离线工具型 Skill。

## 3. 目录结构

```
fireworks-tech-graph/
├── SKILL.md                  # Agent Skill 入口（Codex + Claude Code 共用）
├── agents/openai.yaml        # Codex 界面元数据（display_name/short_description/default_prompt）
├── index.html                # GitHub Pages 产品站（根目录直出，含 3×4 动效总览）
├── site.webmanifest / robots.txt / sitemap.xml   # 站点静态资源
├── scripts/                  # ★ 核心可执行代码（Python + Shell + Node）
│   ├── fireworks.py          # 统一 CLI：doctor/validate/render/check/inspect/export-html/animate/...
│   ├── generate-from-template.py  # JSON → 风格化 SVG 生成器（含 12 风格解析）
│   ├── diagram_ir.py         # 模式化中间表示（schema v1 归一化、typed node/edge IR）
│   ├── validate_svg.py       # SVG 严格校验：xml/markers/geometry/composition
│   ├── fireworks_geometry.py # 几何安全：正交路由、waypoint、桥线、碰撞检测
│   ├── composition_quality.py# 构图质量门禁
│   ├── semantic_contracts.py # 语义契约（Style 9-12：C4/云部署/事件流/可观测性）
│   ├── interactive_html.py   # 离线交互 HTML 导出 + SVG 消毒
│   ├── motion.py             # 动效规划：12 风格 preset、SVG→GIF 渲染、媒体探测
│   ├── svg2gif.js / svg2png.js  # Node/Puppeteer 渲染 worker
│   ├── generate-diagram.sh / validate-svg.sh / test-all-styles.sh
│   └── README.md
├── references/               # ★ 18 份 Agent 提示词风格契约（style-1~12、icons、motion-effects、png-export、composition-quality-contract 等）
├── templates/                # 10 个内置 SVG 模板
├── fixtures/                 # 23 个 JSON 回归 fixture + 1 静态 SVG（Style 8 AI 手绘）
├── examples/                 # 示例
├── tests/                    # 9 个 unittest 测试文件
├── tools/                    # check_project_consistency.py / distribution.py / install-canary.sh
├── assets/                   # 品牌/展示素材（13 GIF + 13 PNG + 2 JSON，含 sponsors）
├── docs/                     # CAPABILITIES.md / ROADMAP.md / releases/
├── schemas/                  # 输入 schema（v1 归一化）
├── skills/fireworks-tech-graph/  # ★ 嵌套 Agent Skill 分发副本（npx skills add 安装的就是它）
├── .github/workflows/        # ci.yml + release.yml
├── package.json              # npm 元数据 + bin + scripts
├── README.md / README.zh.md / CHANGELOG.md / CONTRIBUTING.md / LICENSE
```

## 4. 关键文件职责

| 文件 | 职责 |
|---|---|
| `SKILL.md` | Skill 主指令：图类型分类 → 结构抽取 → 布局规划 → 风格契约加载 → 语义契约 → 形状映射 → 图标查询 → 写 SVG → 校验 → 导出 PNG → 动效 → 视觉复核门禁（最多 2 次修正） |
| `scripts/fireworks.py` | CLI 统一入口（加载 `generate-from-template.py`、`diagram_ir`、`interactive_html`、`motion`、`validate_svg`） |
| `scripts/motion.py` | 动效核心：12 风格 preset、5.75s/115 帧默认时间线、75-vs-115 兼容门禁、媒体探测、GIF 安装 |
| `scripts/svg2gif.js` | Puppeteer/Chromium 渲染 SVG 帧 → GIF 编码 |
| `tools/distribution.py` | 分发一致性检查 + Release 归档构建（`--build-release --output-dir dist`） |
| `tools/install-canary.sh` | 安装冒烟：把 Skill 装到隔离目录验证 `npx skills add` 安装链路 |
| `package.json` | 包元数据、`bin` 映射到 `scripts/fireworks.py`、`files` 白名单（含 `skills/` 嵌套分发） |

## 5. 运行 / 构建 / 发布流程

### 5.1 本地开发与测试

```bash
# 测试（纯 unittest，无第三方框架）
python3 -m unittest discover -s tests -v

# 一致性 + 分发检查
python3 tools/check_project_consistency.py
python3 tools/distribution.py --check

# 依赖（按需）
python3 -m pip install CairoSVG==2.7.1        # PNG 渲染
npm install --no-save puppeteer-core@25.3.0   # GIF 渲染（需 Chromium）
# ffmpeg + imagemagick：GIF 编码与媒体校验
```

### 5.2 CLI 使用

```bash
python3 scripts/fireworks.py doctor            # 环境探测（python/cairosvg/rsvg/node/ffmpeg/motion）
python3 scripts/fireworks.py validate -i in.json --mode architecture   # JSON→IR 校验
python3 scripts/fireworks.py render -i in.json -o out.svg --report r.json
python3 scripts/fireworks.py check --check xml --check geometry out.svg
python3 scripts/fireworks.py inspect out.svg
python3 scripts/fireworks.py export-html -i out.svg -o out.html
python3 scripts/fireworks.py animate -i out.svg -o out.gif --preset auto   # 默认 5.75s/115 帧
```

### 5.3 CI 流水线（.github/workflows/ci.yml）

5 个 job，全部 Ubuntu + Python 3.9/3.12 + Node 20：

1. `quality`：unittest + `bash -n` 脚本语法 + `node --check` JS + `py_compile` Python
2. `render-regression`：CairoSVG 2.7.1 全风格渲染回归
3. `motion-smoke`：ffmpeg+imagemagick 安装、puppeteer-core 25.3.0、12 风格 SVG→GIF 冒烟
4. `distribution`：分发一致性 + `npm pack --dry-run` + 安装 canary
5. `docs-site`：项目一致性检查

### 5.4 发布流程（.github/workflows/release.yml，push v* 标签触发）

1. 校验 `v${GITHUB_REF_NAME}` 与 package.json 版本一致
2. 全量测试 + 全风格渲染回归 + 12 风格动效冒烟
3. `tools/distribution.py --build-release --output-dir dist` 产出发布归档
4. 公开 tag Skill 安装冒烟：`install-canary.sh "yizhiyanhua-ai/fireworks-tech-graph/skills/fireworks-tech-graph#vX.Y.Z"`
5. `gh release create`（附 `docs/releases/vX.Y.Z.md` 说明，`dist/*` 附件）

## 6. 部署兼容性分析

### 6.1 结论：无需传统服务器部署

本项目是**纯离线工具型 Agent Skill**，无后端服务、无数据库、无长连接、无 GUI、无密钥管理。因此：

- **不适用**阿里云混合部署（rsync+venv+Nginx+PM2 那套面向「有数据库/文件系统/长连接/GUI」的服务）
- **不适用**Cloudflare Workers/Pages 运行时（无在线接口需要执行）
- **不涉及**运行时环境变量注入、进程守护、SSL 反向代理

### 6.2 该项目的"部署"= 三种分发通道

| 通道 | 方式 | 说明 |
|---|---|---|
| npm 包 | `npx skills add` / `npm pack` | 包体 = `files` 白名单（SKILL.md + scripts + references + skills/ 嵌套副本等），`bin` 提供 CLI |
| GitHub Release | tag `v*` 自动构建 `dist/*` 归档 | 附件含 12 风格 GIF/PNG 与回归基线 |
| GitHub Pages | 仓库根 `index.html` 静态直出 | 无构建，Actions 中由 `docs-site` job 保证一致性 |

### 6.3 平台兼容性

| 平台 | 兼容性 |
|---|---|
| macOS / Linux | 完整支持（CI 即 Ubuntu；脚本为 bash + Python + Node） |
| Windows | **部分支持**：CLI（Python）可用；但 `*.sh` 脚本（generate-diagram.sh 等）需 Git Bash / WSL；GIF 渲染链路（Puppeteer+Chromium+ffmpeg）在 Windows 需自行安装对应二进制 |
| Codex / Claude Code | 双端官方支持（`SKILL.md` 共用 + `agents/openai.yaml` 可选元数据；`${CLAUDE_SKILL_DIR}` vs Codex 绝对路径两套解析） |

### 6.4 关键运行约束（二次开发须知）

1. **依赖非内建**：无 `requirements.txt`，Python 依赖仅 CairoSVG（PNG）一项为硬性；GIF 链路依赖 Node + Puppeteer-core + Chromium + ffmpeg + ImageMagick，属可选（`doctor` 中 `animation_export` 标记为 `optional`）。
2. **双目录同步**：仓库根目录与 `skills/fireworks-tech-graph/` 是**同一内容的嵌套分发副本**，改任一核心文件（SKILL.md / scripts / references / fixtures）必须同步另一份，CI 的 `tools/check_project_consistency.py` 会校验，不一致即失败。
3. **Skill 根解析**：所有命令块必须自行解析 `SKILL_ROOT`（Claude Code 用 `${CLAUDE_SKILL_DIR}`，Codex 用元数据绝对路径），不得假设 CWD 就是 skill 目录。
4. **版本一致性**：tag 必须等于 `v` + package.json 版本，否则 Release 直接失败。
5. **SVG→GIF 白名单**：仅 12 个已验收 style 语义契约可动效化，其他拓扑失败关闭（fail closed），不得声称任意同风格拓扑可动效。
6. **视觉复核门禁**：生成后必须回读 PNG 人工检查（箭头穿框、标签碰撞、图例遮挡等），最多 2 轮修正；无法读图时如实报告 `visual_review: skipped`，不得猜测。

## 7. 二次开发入口建议

- 新增/调整视觉风格：编辑 `references/style-N-*.md` + `scripts/generate-from-template.py` 的 parse_style + `fixtures/` 增加回归 fixture + `tests/`
- 新增动效契约：`scripts/motion.py`（preset / 签名 / 时间线）+ `references/motion-effects.md`
- 修改 CLI 行为：`scripts/fireworks.py` + 对应模块 + `tests/test_cli.py`
- 改完必须：同步 `skills/fireworks-tech-graph/` 副本 → 本地跑 unittest → `tools/check_project_consistency.py` → `tools/distribution.py --check`
