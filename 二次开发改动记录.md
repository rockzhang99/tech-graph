# 二次开发改动记录

## 改动0001 — 新增 Fireworks Tech Graph 本地 Web 控制台

**提示词：**
> 能不能做成有浏览器服务的界面，我方便使用

**内容：**
在工程根目录新增 `web/` 本地 Web 控制台，用 FastAPI + 原生前端把 Skill 的 CLI 能力包装为浏览器界面：

- 后端 `web/server.py`：提供 `/api/meta`、`/api/doctor`、`/api/examples`、`/api/example/{name}`、`/api/render`、`/api/check`、`/api/inspect`、`/api/png`、`/api/html`、`/api/gif` 等接口，覆盖 JSON→SVG 渲染、四道质量门禁、PNG/GIF/交互 HTML 导出、SVG 语义检查。
- 前端 `web/static/index.html`：三栏布局（配置 / 预览 / 校验导出），支持 14 种图类型切换、12 种风格选择、内置示例加载、JSON 编辑器、拖拽导入、画布缩放/主题切换、质量门禁可视化、GIF 异步任务轮询。
- 启动脚本 `启动控制台.bat`：自动检查并安装 `web/requirements.txt` 依赖，启动 `uvicorn` 并自动打开浏览器。

**新增文件：**
- `web/server.py`
- `web/requirements.txt`
- `web/static/index.html`
- `web/static/style.css`
- `web/static/app.js`
- `启动控制台.bat`

**验证：**
- `python web/server.py --port 8777` 启动成功，首页可访问。
- 全链路冒烟通过：`render → 门禁通过 → PNG 1920×1400 → HTML → GIF 960×700`。

---

## 改动0002 — 修正 Windows 下 cairosvg 可用性误报

**内容：**
原 Skill 的 `doctor` 仅通过 `importlib.util.find_spec("cairosvg")` 判断模块是否存在，导致 Windows 上显示 cairosvg 可用，但实际导出 PNG 时因缺少 `libcairo-2.dll` 崩溃。Web 控制台后端新增 `_cairosvg_usable()`，真正 `import cairosvg` 并渲染一次，捕获 `OSError` 后准确报告 `installed:true, usable:false`，并自动回退到 Chrome headless 截图。

**影响文件：**
- `web/server.py`

---

## 改动0003 — Chrome 截图增加独立 profile 隔离，避免与常驻浏览器冲突

**内容：**
当机器上已有 Chrome 窗口运行时，headless 实例共享默认 user-data-dir 会因单例锁/进程通信而挂起。新增 `_chrome_screenshot()` 每次截图使用 `tempfile.mkdtemp(prefix="ftg-chrome-")` 作为独立 `--user-data-dir`，并补充 `--no-first-run`、`--no-default-browser-check`、`--disable-extensions`、`--disable-sync` 等参数，同时关闭 stdin 避免等待输入。PNG 导出从挂死修复为 3 秒左右完成。

**影响文件：**
- `web/server.py`

---

## 改动0004 — 修复弹窗常驻遮挡导致整页无法点击

**内容：**
`style.css` 中 `.modal { display: grid }` 与 `.canvas-wrap { display: grid }` 的优先级高于 UA 样式表的 `[hidden] { display: none }`，导致带 `hidden` 属性的弹窗仍然全屏显示（`position: fixed; inset: 0`），吞掉页面上所有鼠标点击——渲染按钮、示例下拉、导出按钮全部失效。新增全局规则 `[hidden] { display: none !important; }` 修复。同时把 `.render-bar` 改为 `position: sticky; bottom: 0`，避免编辑器内容过长时渲染按钮被滚出视野。

**影响文件：**
- `web/static/style.css`

---

## 改动0005 — 校正布局报告字段与后端实际返回不一致

**内容：**
前端原按 `report.counts`、`report.routes`、`report.budget` 读取报告字段，与后端实际返回的 `style` / `semantics` / `summary` / `composition.metrics` / `composition.limits` / `issues` 不匹配，导致报告区几乎空白。重写 `renderReport()`：展示视觉风格、语义契约与校验状态、节点/连线规模、画布尺寸、构图评分（实测值对照上限值）、问题清单。

**影响文件：**
- `web/static/app.js`

---

## 改动0006 — 启动脚本改纯 ASCII（文件名 + 内容），修复 cmd 乱码

**提示词：**
> PS D:\4Project\tech-graph> .\启动控制台.bat
> 'works' 不是内部或外部命令...（大量乱码被当作命令执行）
> 如果有中文问题，那就改为英文吧

**根因：**
脚本以 UTF-8 无 BOM 保存，而 cmd 默认按 GBK(936) 解码，所有中文提示被解码成乱码，且带引号/管道的行被拆成多条"命令"执行，刷出大量 `'xxx' 不是内部或外部命令`。服务本身能起来，但控制台完全不可读。

**内容：**
1. `启动控制台.bat` 全部提示文案改为英文，文件现在**零非 ASCII 字节**（已校验 `nonAsciiBytes=0`）。
2. 文件名 `启动控制台.bat` → **`start.bat`**。中文文件名在跨进程/跨工具传递时同样会被编码打乱（本次排查中自动化工具传递该文件名就出现 `鍚姩鎺у埗鍙bat`），改为 ASCII 名彻底根除。
3. `web/server.py` 启动横幅由「Skill / Chrome / 控制台」改为纯 ASCII 的 `Skill / Chrome / Console`，避免非 UTF-8 控制台下乱码（HTTP 返回给前端的中文错误详情保持不变，前端是 UTF-8，显示正常）。

**验证：**
- `start.bat` 运行后控制台输出全英文，无任何乱码；服务正常启动（`/api/meta` 返回 14 类型 / 12 风格）。

**影响文件：**
- `启动控制台.bat` → 重命名为 `start.bat`
- `web/server.py`

---

## 改动0007 — 生成 README 与 LICENSE，提交并推送

**提示词：**
> 生成我的readme并提交推送git

**内容：**
1. 新增 `README.md`：项目定位、功能表、环境要求、快速启动、使用流程、图规格 JSON 示例、项目结构、12 个 HTTP 接口清单、Windows 已知坑（cairosvg 误报 / Chrome 挂死 / cmd 乱码）、许可与作者信息。
2. 新增 `LICENSE.md`：MIT，版权归 yoruaki（按 dev-rules 规范，每个项目应含 README/LICENSE/技术规范/改动记录四件套）。
3. 新增 `二次开发改动记录.md` 本条记录。

**提交与推送：**
- 按 dev-rules 规范新建工作分支 `docs/readme`，提交后推送分支，再快进合并回 `main` 并推送。
- commit 采用 Angular 风格：`docs: 新增 README 与 LICENSE，补充项目说明`。

**验证：**
- 推送后 `origin/main` 与本地 `main` 指向同一 commit，工作区干净。

**影响文件：**
- `README.md`（新增）
- `LICENSE.md`（新增）
- `二次开发改动记录.md`（追加本条）

---

## 改动0008 — 新增「提示词生成」：自然语言 → 图

**提示词：**
> 有没有一种方式是我选图类型，选视觉风格，第三步是我给提示词，他生成json，而不是只能选这几个示例呢？

**核心设计：语义与几何分离**

大模型擅长理解语义，但不擅长算数。直接让它输出坐标几乎必然撞上间距、路由、标签门禁。
因此分工为：

- **大模型 / 本地解析** 只输出「语义蓝图」：有哪些节点、谁连谁、什么含义、如何分层
- **`web/layout.py` 本地布局引擎** 确定性计算坐标、尺寸、端口方向

这样既稳定可控，又不依赖模型做算术。生成后跑完整校验，未通过先放宽间距重排，
仍失败且配置了模型时把错误回传要求修正（最多一轮语义级修正）。

**新增文件：**
- `web/layout.py` — 布局引擎。泳道（横向带）排布、自适应行打包、拓扑分层、端口方向推导、
  图例生成、Style 9-12 语义字段注入
- `web/llm.py` — OpenAI 兼容客户端（用标准库 urllib，零额外依赖）、提示词与 few-shot 构建、
  稳健 JSON 提取（兼容 ```json 代码块与前后说明文字）、模型列表拉取
- `web/heuristic.py` — 本地启发式解析兜底。识别「A → B → C」链路与并列元素，
  按关键词推断图形种类（18 种）与连线语义（7 种），无需 API Key 即可用

**修改文件：**
- `web/server.py` — 新增 `/api/generate`、`/api/config`、`/api/config/test`、`/api/config/models`；
  配置持久化到 `~/.codebuddy/fireworks-tech-graph-config.json`（不进 Git），
  支持环境变量 `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL` 覆盖
- `web/static/index.html` — 左栏新增第 3 步「提示词生成」卡片 + 模型设置弹窗
- `web/static/style.css` — 新增提示词区、表单控件、开关样式
- `web/static/app.js` — 生成流程、配置读写、连接测试、模型拉取

**过程中修掉的两个布局 bug：**
1. **缩放未计入间隙**：节点总宽超出容器时按比例缩小，但间隙是在缩放后又加上的，
   导致节点整体溢出容器右边界（表现为 `CONTAINER_GUTTER = -27`）。
   改为先压缩间隙、仍放不下才等比缩小节点，并设 0.72 缩放下限。
2. **单行硬塞**：5 个长英文标签横排在 960 宽度放不下，缩到下限仍溢出 viewBox
   （`canvas_clip: exceeds viewBox`）。改为自适应行打包 `_pack_rows()`：
   从「尽量少分行」开始尝试，只要节点会被压扁就增加行数。
   同时修正端口方向——跨行走垂直、同行才走水平，避免折行后出现反向折线。

**风格支持范围的取舍：**
Style 9-12 是工程评审专用风格，领域契约要求结构化数据（C4 的 scope/rough_seed/legend、
Cloud 的 platform_profile/icon manifest、Event 的 topics rails、Ops 的黄金信号与 trace 瀑布）。
通用布局引擎无法产出符合其设计意图的图，因此明确标注不支持提示词生成，
界面上禁用生成按钮并引导改用内置示例或手写 JSON——而不是产出勉强过校验但不专业的图。
Style 1-7 完整支持，Style 8 为 AI 手绘风本就不接受 JSON。

**验证：**
- 布局引擎 8 组提示词 × 多图类型：全部通过四道门禁（xml / markers / geometry / composition）
- 风格矩阵：Style 1-7 全部 PASS；9-12 按预期返回明确引导
- 错误处理：空提示词 400、Style 8 被拒、Style 10 生成按钮自动禁用
- 完整 UI 流程（Puppeteer 真实鼠标操作）：提示词生成 → 门禁 4/4 → 导出按钮启用 →
  模型设置弹窗开关，零 JS 错误

**影响文件：**
- `web/layout.py`（新增）
- `web/llm.py`（新增）
- `web/heuristic.py`（新增）
- `web/server.py`
- `web/static/index.html`
- `web/static/style.css`
- `web/static/app.js`
- `README.md`
- `二次开发改动记录.md`（追加本条）

---

## 改动0009 — 新增「AI 应用层 + 模型层」架构图规格 `ai-stack.json`

**提示词：**
> 换个思路，你作为一个ai工程师，画出来AI应用层和模型层的架构图json给我

**背景：**
用户希望拿到一张完整的企业级全栈技术架构图 JSON（参照一张网络截图），但该截图分辨率过低（512×1080），
本地 OCR（easyocr / PaddleOCR 均未装 paddlepaddle 依赖）识别结果全部为乱码，无法还原图中细项。
因此改为由 AI 工程师视角**重新设计**这两层的架构，而不是照抄看图。

**设计（11 条泳道 × 3 节点，共 33 节点 / 48 连线）：**

| 层 | 泳道 | 节点 |
|---|---|---|
| AI 应用层 | 交互入口 | 用户 / 业务方 · 对话门户 · 业务插件 |
| AI 应用层 | 编排与 Agent | 提示词工程 · Agent 编排 · 工作流引擎 |
| AI 应用层 | 记忆与上下文 | 长期记忆 · 短期记忆 · 上下文压缩 |
| AI 应用层 | 知识增强 RAG | 文档解析 · 混合检索 · 结果重排 |
| AI 应用层 | 工具与执行 | MCP 工具 · 工具调用 · 代码沙箱 |
| AI 应用层 | 网关与治理 | 语义缓存 · AI 网关 · 护栏合规 |
| 模型层 | 模型服务 | 模型路由 · 推理服务 · 流式输出 |
| 模型层 | 商业大模型 | 聚合比价 · 海外闭源 · 国内商用 |
| 模型层 | 开源与自建 | 开源基座 · 微调训练 · 蒸馏量化 |
| 模型层 | 专用模型 | Embedding · Rerank 模型 · 多模态模型 |
| 模型层 | 评测与基础设施 | 模型评测 · 模型仓库 · GPU 集群 |

连线语义：层内横向为「处理链」（如 文档解析 → 混合检索 → 结果重排），
层间纵向按列对齐为「层间调用」，主链路（中列）带标签：发起请求 → 读写记忆 → 检索知识 → 结果入上下文 → 发起推理 → 调用模型 → 推理。

**几何约定（沿用 `fullstack-lite.json` 的 proven 参数）：**
画布 1800×2120；泳道 x=50 / width=1700 / height=130 / 步进 160（y 从 110 起）；
节点 width=140 / height=60，节点 y = 泳道 y + 30，三列 x = 300 / 830 / 1360。

**踩坑：**
1. `user_avatar` 图形左侧有约 64px 头像占位，标签可用宽度只剩约 76px。
   初版 `用户 / 业务方` 实测渲染宽度 113.8px 会溢出节点框。
   修法：**加宽单个节点到 200 并保持中心不变**（x 270 → 470，中心 370 与其余列对齐），
   而不是缩短标签——这样既不破坏列对齐，语义也不丢失。
2. Skill 的 `report.text_metrics` 只是字符串 `"heuristic-v1"`，**不提供文字溢出检测**。
   门禁全 PASS 也可能出现文字溢出节点，需自行按「CJK 计 1 字宽、ASCII 计 0.56 字宽」估算校验。

**验证：**
- 渲染 + 四道门禁：xml / markers / geometry / composition **全部 PASS**
- 构图评分 **100**，`total_bends 0`、`bridged_crossings 0`、`max_route_stretch 1.0`、
  `minimum_node_gap 100.0`、`minimum_container_gutter 30.0`、`issues []`
- 文本溢出检查：33 个节点标签全部无溢出（`text fit OK`）
- 已导出预览：`output/ai-stack.svg`、`output/ai-stack.png`（1800×2120）

**新增文件：**
- `ai-stack.json`（工程根目录，图规格）
- `output/ai-stack.svg`
- `output/ai-stack.png`

**说明：**
本轮用于验证的临时脚本（`_validate_spec.py` / `_text_fit.py` / `_export_png.py`）跑完即删，未留在仓库。

---

## 改动0010 — 支持导入外部 elements 格式图描述，并修复 GBK 文件导入

**提示词：**
> 我这个json文件，他只渲染出来标题

**根因（两个独立问题叠加）：**

1. **Schema 不匹配**。用户提供的 JSON 是第三方绘图工具的导出格式：顶层 `{title, diagramX, diagramY, elements:[...]}`，
   元素用 `tag` 区分 `Textbox / Group / Shape / Icon / Relationship`，坐标挂在 `x/y/width/height` 与相对 `points` 上。
   fireworks-tech-graph 只识别 `containers / nodes / arrows` 三个顶层数组，其余字段**一律静默忽略**——
   于是 `elements` 整个被丢掉，只剩顶层 `title` 进了标题块，表现就是「只渲染出标题」。

2. **文件是 GBK 编码**。实测该文件首字节序列为 `\xd6\xc6\xd4\xec`（GB18030 的「制造」），UTF-8 解码直接抛
   `UnicodeDecodeError`。而 `File.text()` 永远按 UTF-8 解码且不报错，只会返回替换字符，
   随后 `JSON.parse` 失败或得到乱码内容。

**内容：**

1. 新增 `web/convert.py` — 外部 elements 格式 → FTG v1 spec 转换器：

   | 外部 tag | 目标 | 说明 |
   |---|---|---|
   | `Group` | `containers[]` | label 取 `title.text`，`bgColor`/`borderColor` 映射为 `fill`/`stroke` |
   | `Shape` | `nodes[]` | 首行 `texts[0]` 为 label，其余合并为 sublabel；`rectangle→rect`、`cylinder→cylinder` |
   | `Icon` | `nodes[]` | 50×50 的图标放大为 152×48 矩形并保持中心不变，否则文字放不下 |
   | `Textbox` | 画布标题 / 副标题 | 不单独渲染，交给 FTG 自带的标题块（去掉 Markdown 井号） |
   | `Relationship` | `arrows[]` | 见下方取舍 |

   **关键取舍 — 箭头不走 source/target**：`DiagramIR.normalize_diagram` 要求箭头两端必须是 node id
   （否则 `references unknown node`），而外部格式的连线挂在 Group（容器）上，无法直接映射。
   改为 `x1/y1/x2/y2` + `route_points` 的**纯坐标连线**——渲染器 `render_arrow` 在
   `source is None` 时直接使用坐标端点，视觉上也最贴近原图。

   **坐标归一化**：原图坐标系任意（本例 1170×1199），等比缩放到目标宽度 1080 后整体平移；
   顶部固定预留 118px，因为 `render_title_block` 在 y≈56 处画标题且**不会自动下移内容**，不预留会压住第一层。

2. `web/server.py` — `/api/render` 增加自动识别：`convert.needs_conversion()` 命中（有 `elements`
   且无 `nodes`/`containers`）时先转换再渲染；转换失败返回 `400 [convert] ...`；
   响应新增 `converted` 标志，命中时回传翻译后的 `spec`。

3. `web/static/app.js` — 新增 `readTextFile()`：按 BOM 判断 UTF-16，再用
   `TextDecoder("utf-8", {fatal:true})` 解码，失败回退 `gb18030`；`handleSvgFile` 与 `handleSpecFile`
   改用该函数。渲染成功且 `result.converted` 时，把翻译后的 spec 写回编辑器，方便继续微调。

**验证：**
- 用户提供的 6.json（GB18030，1087 行）：`6 containers / 31 nodes / 6 arrows`，画布 1176×1229
- 四道门禁 **全部 PASS**：`xml / markers / geometry / composition`
- 端到端 HTTP 冒烟：`POST /api/render` 返回 `converted:true, passed:true`

**影响文件：**
- `web/convert.py`（新增）
- `web/server.py`
- `web/static/app.js`
- `README.md`

**已知限制：**
外部格式里的 `icon` 名称（trending-up / cpu / kubernetes 等）无法保留——FTG 的节点只有少数内置图形
（rect / cylinder / hexagon / document / folder / terminal 等），没有通用图标表，转换后统一退化为矩形节点。

---

## 改动0011 — 节点越出容器：自动收回 + 报错翻译

**提示词：**
> [layout] COMPOSITION_QUALITY: CONTAINER_GUTTER:a1@g_access=-16.35>0.0; CONTAINER_GUTTER:e1@g_edge=-16.35>0.0

**根因：**
构图质量门禁 `composition_quality.assess_composition` 会对每个节点计算它与「中心所落入的容器」的四边留白
（`container_gutter` = min(左, 上, 右, 下)），低于 `min_container_gutter` 就报 `CONTAINER_GUTTER`。
`standard` profile 的下限是 **0.0**，所以报负数 = **节点矩形真的越出了容器边界**（本例右边越出 16.35px），
`showcase` profile 下限是 20.0，则会因为「贴太近」而报错。

原始报错只有一个负数，既看不出是哪条边越界，也看不出该改 `x/y` 还是 `width/height`，排查成本很高。

**内容：**

1. 新增 `web/autofit.py`：
   - `gutter_violations()` — 用正则从门禁报错里解析出 `(节点 id, 容器 id, 实测留白, 要求下限)`。
   - `explain()` — 把报错翻译成可执行的坐标提示，含具体数值与两种改法：
     ```
     节点 a1 的右边越出容器 g_access 16.35px（a1.x + width = 936.35，g_access 对应边界 = 920.0）
       → 把 x 改为 ≤ 715.65 或 width 改为 ≤ 172.0
     ```
   - `fit_into_containers()` — 自动回收。策略是**先平移、放不下才缩小**：
     先按目标留白把 `width/height` 压到能放下（不低于 `MIN_WIDTH=90` / `MIN_HEIGHT=40`，再窄文字必然溢出），
     再把 `x/y` 夹进合法区间；容器本身小到装不下时改为居中而不是硬缩。

2. `web/server.py` — `/api/render` 在 `build_svg_with_report` 抛 `ValueError` 时：
   若含 `CONTAINER_GUTTER` 违规则按报错里的实际下限自动修复后**重试一次**，成功则响应带
   `autofit` 改动明细并回写修正后的 spec；仍失败才抛出 `explain()` 翻译过的提示。
   非 gutter 类错误保持原样抛出。

3. `web/static/app.js` — 用 `result.spec` 统一回写（转换 / 自动修复都走这条路）；
   命中自动修复时 toast 提示「已自动收回 N 个越界节点」。

**关于留白目标取 8px 而不是 0：**
门禁只要求 ≥0，但贴着容器边缘画很容易连带触发 `min_label_clearance` 等相邻规则，
一次修到位比反复弹错更省事。下限会随 `quality_profile` 变化，因此 server 侧取
`max(8.0, 报错里的实际下限)`，showcase 规格也能一次修好。

**验证（构造复现用例，节点右边越出容器 16.35px）：**
- 原始渲染报出与用户完全一致的错误：`CONTAINER_GUTTER:a1@g_access=-16.35>0.0; CONTAINER_GUTTER:e1@g_edge=-16.35>0.0`
- `explain()` 准确定位到右边、给出 `x ≤ 715.65` / `width ≤ 172.0` 两种改法
- 自动回收后四道门禁 **全部 PASS**，构图评分 **100**（`minimum_container_gutter` 从 -16.35 → 8.0）
- HTTP 端到端：`autofit` 明细正确回传，编辑器收到的 spec 中 `a1.x` 已修正为 715.65

**影响文件：**
- `web/autofit.py`（新增）
- `web/server.py`
- `web/static/app.js`
- `README.md`
